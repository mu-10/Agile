import React from "react";
import { View, Text } from "react-native";
export default function MapNative() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Map is web-only for now.</Text>
    </View>
  );
}
